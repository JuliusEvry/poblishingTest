@model Microsoft.EntityFrameworkCore.DbSet<Domain.Entities.TestTable>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Id : @item.Value</li>
        }
    </ul>
</div>
